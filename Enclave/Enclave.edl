enclave {
     
    trusted {
	public int spm_send_cmd(int fd, [in, size=100] char* buffer, int node_size, [in, size=4]char* response, int pid, [in, size=16] int*sp);
    };

    untrusted {
	int enc_rdafwr([in, size=20]  int*ds_param, [in, size=8192] char* u_buf, [in, out, size=4] char* response, int count);
    };

};
